#include <bits/stdc++.h>

using namespace std;

int main()
{
    char n;
    string k = "";
    while (cin >> n)
    {
        if (isupper((int)n))
        {
            cout << n;
        }
    }

    return 0;
}